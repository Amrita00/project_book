<div class="container-fluid">

    <div class="row">
        <div class="view">
            <img class="poster" src="{{ asset('images/' ~ (view.getImage())) }}">
        </div>
        <div class="col-md-8">

            <table  class="ui celled structured table">
                <tr>
                    <th>Book Name</th>
                    <th>Description</th>
                    <th>Author</th>
                    <th>Rent</th>

                </tr>
                <tr>
                    <td>{{ view.name }}</td>
                    <td>{{ view.description }}</td>
                    <td>{{ view.author }}</td>
                    <td><a>Rent</a></td>
                </tr>
            </table>
        </div>
    </div>
</div>